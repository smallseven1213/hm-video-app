import 'package:flutter/material.dart';

class LayoutUserScreen extends StatelessWidget {
  const LayoutUserScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
