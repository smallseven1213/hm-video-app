import 'package:flutter/material.dart';
import 'package:shared/screens/game/deposit_page_list.dart';

class GameDepositPageListScreen extends StatelessWidget {
  const GameDepositPageListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const GameDeposit();
  }
}
