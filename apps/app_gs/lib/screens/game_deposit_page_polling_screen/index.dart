import 'package:flutter/material.dart';
import 'package:shared/screens/game/deposit_page_polling.dart';

class GameDepositPagePollingScreen extends StatelessWidget {
  const GameDepositPagePollingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const GameDeposit();
  }
}
