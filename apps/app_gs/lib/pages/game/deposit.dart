import 'package:flutter/material.dart';

class GameDeposit extends StatelessWidget {
  const GameDeposit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('GameDeposit~'),
      ),
      body: Center(
        child: Text('GameDeposit'),
      ),
    );
  }
}
