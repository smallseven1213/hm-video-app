import 'package:flutter/material.dart';

class ShareRecord extends StatelessWidget {
  const ShareRecord({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ShareRecord'),
      ),
      body: const Center(
        child: Text('ShareRecord'),
      ),
    );
  }
}
