import 'package:flutter/material.dart';

class FavoritesActorScreen extends StatelessWidget {
  const FavoritesActorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('FavoritesActorScreen'),
      ),
    );
  }
}
