import 'package:flutter/material.dart';

class FavoritesVideoScreen extends StatelessWidget {
  const FavoritesVideoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('FavoritesVideoScreen'),
      ),
    );
  }
}
