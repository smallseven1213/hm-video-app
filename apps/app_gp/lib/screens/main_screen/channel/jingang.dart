// Jingang

import 'package:flutter/material.dart';

class Jingang extends StatelessWidget {
  final int channelId;
  const Jingang({Key? key, required this.channelId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
    );
  }
}
