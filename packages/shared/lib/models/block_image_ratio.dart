enum BlockImageRatio {
  block1(182 / 102),
  // block2(),
  block3(182 / 102),
  block4(119 / 159),
  // block5(),
  // block6(),
  block10(182 / 248);

  final double ratio;
  const BlockImageRatio(
    this.ratio,
  );
}
