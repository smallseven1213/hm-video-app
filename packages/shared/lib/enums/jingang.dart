enum OuterFrame {
  border(true),
  noBorder(false);

  final bool value;
  const OuterFrame(this.value);
}

enum OuterFrameStyle {
  none,
  circle,
  square,
}

enum JingangStyle {
  none,
  single,
  multiLine,
}
